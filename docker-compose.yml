version: "3"
services:
  app:
    build: .
    restart: always
    ports:
      - "3010:3010"
    environment:
      - FLASK_DEBUG=0
    volumes:
      - cache-data:/app/cache

volumes:
  cache-data:
